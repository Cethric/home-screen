---
apiVersion: v1
kind: Service
metadata:
  name: homescreen-web-slideshow
spec:
  type: ClusterIP
  selector:
    app: homescreen-web-slideshow
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  - name: https
    port: 8443
    targetPort: 8443

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homescreen-web-slideshow-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: cors-headers
spec:
  rules:
    - host: "homescreen-web-slideshow.home-automation.cloud"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: homescreen-web-slideshow
              port:
                name: http
